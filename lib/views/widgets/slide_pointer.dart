import 'package:flutter/material.dart';

class SlidePointer extends StatelessWidget {
  const SlidePointer({Key? key, this.index}) : super(key: key);

  final int? index;

  @override
  Widget build(BuildContext context) {
    return Container(
        // height: 4,
        // width: ,
        );
  }
}
